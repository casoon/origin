[package]
name = "xtask"
description = "Entry point for `cargo xtask`. The tasks live in origin-xtask."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
origin-xtask = { workspace = true }
